const authContact = (state, {type, payrole})=>{
    switch (type) {
        case "GET_CONTACTS":
            return state;
        default:
            return state;
            break;
    }
}
export default authContact;